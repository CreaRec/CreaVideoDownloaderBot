node_modules/
dist/

# Local files (not used by the running app).
# App secrets live in config/settings.json on the server.
# Deploy is via GitHub Actions on push to main.
.env
.env.local
.env.*.local

# Local runtime settings contain Telegram credentials and sessions.
config/settings.json

# Local app state (delete-button tokens, etc.).
data/

# Local logs and OS/editor noise.
*.log
.DS_Store
