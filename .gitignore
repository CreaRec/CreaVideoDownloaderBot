node_modules/
dist/

# Local deploy credentials (SSH/sudo for scripts/deploy.sh).
.env
.env.local
.env.*.local

# Local runtime settings contain Telegram credentials and sessions.
config/settings.json

# Local app state (delete-button tokens, etc.).
data/

# Local logs and OS/editor noise.
*.log
.DS_Store
